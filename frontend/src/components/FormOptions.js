import React from 'react';

const FormOptions = () => (
  <div style={{ width: '200px', padding: '10px', borderLeft: '1px solid gray' }}>
    <h4>Component Options</h4>
    <p>Configure options for selected component</p>
    {/* Add custom settings for each component here */}
  </div>
);

export default FormOptions;
